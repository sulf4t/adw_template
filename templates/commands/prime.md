# Prime

Understand this repository before doing anything else, then summarize what you learned.

## Run
- `git ls-files | head -300`
- `git log --oneline -15`

## Read
- `README.md`
- `CLAUDE.md` or `AGENTS.md`, if present
- `.claude/commands/conditional_docs.md`
- The package manifest that exists: `pyproject.toml`, `package.json`, `Makefile`, `go.mod`, `Cargo.toml`, `docker-compose.yml`

## Report
In at most 15 lines: what the project does, the stack, how to install, run and test it, the five most important directories or files. Mention anything that looks broken or missing (no README, no tests, secrets committed).
