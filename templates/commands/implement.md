# Implement a plan

Follow the `Instructions` to implement the `Plan`, then `Report`.

## Instructions
- Read the plan. Think hard about it. Implement every step in order.
- Do only what the plan says. If the plan is wrong or impossible, stop and explain why instead of improvising.
- Run the plan's validation commands at the end.

## Plan
$ARGUMENTS

## Report
- What you did, as a concise bullet list.
- The output of `git diff --stat`.
- The result of each validation command.
